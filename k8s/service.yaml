apiVersion: v1
kind: Service
metadata:
  name: hello-microservice-service
spec:
  selector:
    app: hello-microservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
